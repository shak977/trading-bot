# Secrets — NEVER commit these
.env
*.env
!.env.example

# Generated output (rebuilt on each run; fine to ignore)
dashboard.html
signals.json
logs/

# Python
__pycache__/
*.pyc
.venv/
venv/
_site/

# Scratch / tooling (never commit)
_check.js
_check*.js
# ignore local Claude settings, but DO version installed skills (sync across machines)
.claude/*
!.claude/skills/
.wrangler/
# cloned third-party sources (e.g. tradermonty/claude-trading-skills)
vendor/
